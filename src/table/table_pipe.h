#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <assert.h>
#include <sys/stat.h>

#include "table.h"

#define PIPE_NAME pipe

int fd[2];

int StartTableWork (const int process)
{
    switch (process)
    {
        case WASHER_P:
            {

            }
    }

    return 0;
}

int 